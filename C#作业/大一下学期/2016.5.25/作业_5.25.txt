1、
    实现Windows窗体应用程序，具有读写文本文件的功能，详细功能如下：
    界面上放置1个TextBox，用来获得用户输入的文件夹路径，按回车表示输入结束；
    放置5个Button，功能分别为另存为文件、写入文件、读取文件、读取下一条记录、退出应用程序；
    写入文件的内容为TextBox中用户输入的文件夹路径下的所有子文件夹，包括完整的磁盘路径；
    读取和写入的记录为其中的一个子文件的完整路径；
    记录之间不分行，读取和写入记录的时候中间用“#”来间隔。